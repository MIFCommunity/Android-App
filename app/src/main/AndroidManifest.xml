<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.marketinvestorfund.mifcommunity">
    android:versionCode="1"
    android:versionName="1.0" >
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <!-- <uses-permission android:name="android.permission.GET_ACCOUNTS" /> -->
    <!-- <uses-permission android:name="android.permission.READ_PROFILE" /> -->
    <!-- <uses-permission android:name="android.permission.READ_CONTACTS" /> -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:parentActivityName=".AppHomeActivity"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <meta-data
            android:name="android.support.PARENT_ACTIVITY"
            android:value="com.marketinvestorfund.mifcommunity.AppHomeActivity" />

        <activity
            android:name=".AppHomeActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AppAuthActivity">

            <!-- android:theme="@style/AppTheme.AppBarOverlay"> -->
            <intent-filter>
                <action android:name="android.intent.action.AUTH" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AppSettingsActivity">

            <!-- android:theme="@style/AppTheme.AppBarOverlay"> -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.marketinvestorfund.mifcommunity.AppHomeActivity" />

            <intent-filter>
                <action android:name="android.intent.action.AUTH" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
         <activity
             android:name=".AppAuthWindowActivity"
             android:label="@string/title_activity_app_auth_window"
             android:parentActivityName=".AppHomeActivity"
             android:theme="@style/AppTheme.NoActionBar">
             <meta-data
             android:name="android.support.PARENT_ACTIVITY"
             android:value="com.marketinvestorfund.mifcommunity.AppHomeActivity" />

             <intent-filter>
             <action android:name="android.intent.action.AUTH" />

             <category android:name="android.intent.category.LAUNCHER" />
             </intent-filter>
         </activity>
        -->


        <!-- <activity -->
        <!-- android:name=".AppAuthFullActivity" -->
        <!-- android:configChanges="orientation|keyboardHidden|screenSize" -->
        <!-- android:label="@string/title_activity_app_auth_full" -->
        <!-- android:parentActivityName=".AppHomeActivity" -->
        <!-- android:theme="@style/AppTheme.PopupOverlay"> -->
        <!-- <meta-data -->
        <!-- android:name="android.support.PARENT_ACTIVITY" -->
        <!-- android:value="com.marketinvestorfund.mifcommunity.AppHomeActivity" /> -->


        <!-- <intent-filter> -->
        <!-- <action android:name="android.intent.action.SUB" /> -->


        <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
        <!-- </intent-filter> -->
        <!-- </activity> -->

        <activity
            android:name=".AppPrivacyActivity"
            android:label="@string/title_activity_app_privacy"
            android:parentActivityName=".AppHomeActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.marketinvestorfund.mifcommunity.AppHomeActivity" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>